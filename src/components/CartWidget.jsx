import cartIcon from '../img/cart-icon.png'

const CartWidget = () => {
    return (
        <div>
            <img src={cartIcon} alt='' width='30' height='30'></img>
        </div>
    );
};

export default CartWidget;